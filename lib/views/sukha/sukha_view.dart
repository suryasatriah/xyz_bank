import 'package:flutter/material.dart';

class SukhaView extends StatelessWidget {
  const SukhaView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
